'use client';import {FormEvent,useEffect,useState} from 'react';import Link from 'next/link';
export default function AdminSetup(){const [ready,setReady]=useState<boolean|null>(null),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[secret,setSecret]=useState(''),[notice,setNotice]=useState('');useEffect(()=>{fetch('/api/auth/setup',{cache:'no-store'}).then(r=>r.json()).then(d=>setReady(Boolean(d.ready))).catch(()=>setReady(false))},[]);async function submit(e:FormEvent){e.preventDefault();setNotice('Memproses…');const r=await fetch('/api/auth/setup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password,secret})});const d=await r.json();setNotice(r.ok?'Administrator berhasil dibuat. Silakan masuk.':d.error||'Aktivasi gagal.');if(r.ok)setReady(true)}if(ready===null)return <main className="auth-shell"><div className="auth-card">Memeriksa kesiapan Administrator…</div></main>;if(ready)return <main className="auth-shell"><div className="auth-card"><span className="eyebrow">ADMINISTRATOR</span><h1>Aktivasi sudah selesai</h1><p>Administrator pertama sudah dibuat. Halaman ini tidak dapat digunakan untuk membuat Administrator tambahan.</p><Link className="btn btn-primary" href="/admin/login">Masuk sebagai Administrator</Link></div></main>;return <main className="auth-shell"><form className="auth-card auth-form" onSubmit={submit}><span className="eyebrow">AKTIVASI PRIVAT</span><h1>Aktifkan Administrator pertama</h1><p>Gunakan kode rahasia yang disimpan di Environment Variables Netlify. Aktivasi hanya dapat dilakukan satu kali.</p><label><span>Nama</span><input value={name} onChange={e=>setName(e.target.value)} required/></label><label><span>Email</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label><span>Kata sandi</span><input type="password" minLength={12} value={password} onChange={e=>setPassword(e.target.value)} required/></label><label><span>Kode aktivasi privat</span><input type="password" value={secret} onChange={e=>setSecret(e.target.value)} required/></label>{notice&&<p>{notice}</p>}<button className="btn btn-primary" type="submit">Aktifkan Administrator</button></form></main>}
