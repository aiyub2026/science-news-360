import {NextResponse} from 'next/server';import {login} from '@/lib/auth/server';import {sameOrigin} from '@/lib/security';
export const runtime='nodejs';export async function POST(req:Request){if(!sameOrigin(req))return NextResponse.json({error:'Permintaan tidak diizinkan.'},{status:403});try{const {email,password,adminOnly}=await req.json();const result=await login(email,password,Boolean(adminOnly));const res=NextResponse.json({ok:true,user:result.user});res.cookies.set('sn360_session',result.token,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:result.maxAge});return res}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Login gagal.'},{status:401})}}
