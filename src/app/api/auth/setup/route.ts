import {NextResponse} from 'next/server';import {bootstrapAdministrator,hasAdministrator} from '@/lib/auth/server';import {sameOrigin} from '@/lib/security';
export const runtime='nodejs';export async function GET(){return NextResponse.json({ready:await hasAdministrator()},{headers:{'Cache-Control':'no-store'}})}export async function POST(req:Request){if(!sameOrigin(req))return NextResponse.json({error:'Permintaan tidak diizinkan.'},{status:403});try{return NextResponse.json({ok:true,user:await bootstrapAdministrator(await req.json())},{status:201})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Aktivasi Administrator gagal.'},{status:400})}}
