import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {listSecurityAudit,listUsers,sessionUser,updateUserByAdmin} from '@/lib/auth/server';import {hasCapability} from '@/lib/auth/permissions';import {sameOrigin} from '@/lib/security';
export const runtime='nodejs';async function admin(){const jar=await cookies();const u=await sessionUser(jar.get('sn360_session')?.value);return u&&hasCapability(u.role,'MANAGE_USERS')?u:null}export async function GET(){if(!await admin())return NextResponse.json({error:'Akses ditolak.'},{status:403});return NextResponse.json({users:await listUsers(),audit:await listSecurityAudit()},{headers:{'Cache-Control':'no-store'}})}export async function PATCH(req:Request){if(!sameOrigin(req))return NextResponse.json({error:'Permintaan tidak diizinkan.'},{status:403});if(!await admin())return NextResponse.json({error:'Akses ditolak.'},{status:403});try{const {id,...patch}=await req.json();return NextResponse.json({ok:true,user:await updateUserByAdmin(id,patch)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Perubahan gagal.'},{status:400})}}
