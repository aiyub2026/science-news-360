import {NextResponse} from 'next/server';import {getAnalytics,recordVisit} from '@/lib/analytics/server';export const runtime='nodejs';export const dynamic='force-dynamic';export async function GET(){return NextResponse.json(await getAnalytics(),{headers:{'Cache-Control':'no-store'}})}export async function POST(req:Request){try{const {path='/',session='',heartbeat=false}=await req.json();if(!session)return NextResponse.json({error:'Session diperlukan.'},{status:400});return NextResponse.json(await recordVisit(String(path).slice(0,300),String(session).slice(0,120),Boolean(heartbeat)))}catch{return NextResponse.json({error:'Analytics gagal.'},{status:400})}}
