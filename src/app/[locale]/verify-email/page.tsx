'use client';import Link from 'next/link';import {useAuth} from '@/components/auth/AuthProvider';
export default function Verify(){const {user,verifyEmail}=useAuth();return <div className="auth-shell"><div className="auth-card enterprise-auth"><span className="eyebrow">VERIFIKASI EMAIL</span><h1 className="serif">Verifikasi alamat email</h1><p className="summary">Klik tombol berikut untuk mengaktifkan akun dan melanjutkan pengisian profil.</p><button className="btn btn-primary" onClick={verifyEmail}>Verifikasi email sekarang</button><Link className="btn btn-secondary" href="/id/profile-completion">Lanjutkan ke profil</Link><small>{user?.email||'Akun pengguna'}</small></div></div>}
