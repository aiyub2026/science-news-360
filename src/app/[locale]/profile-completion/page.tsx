'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {useAuth} from '@/components/auth/AuthProvider';
export default function Profile(){const {user,completeProfile}=useAuth(),router=useRouter();const [name,setName]=useState(user?.name||''),[orcid,setOrcid]=useState(user?.orcid||''),[institution,setInstitution]=useState(user?.institution||'');function submit(e:FormEvent){e.preventDefault();completeProfile({name,orcid,institution});router.push('/dashboard/contributor')}return <div className="auth-shell"><div className="auth-card wide-auth"><span className="eyebrow">PROFILE COMPLETION</span><h1 className="serif">Lengkapi profil akademik</h1><form className="auth-form two-col" onSubmit={submit}><label>Nama profesional<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>ORCID iD<input value={orcid} onChange={e=>setOrcid(e.target.value)} placeholder="0000-0000-0000-0000"/></label><label>Afiliasi<input value={institution} onChange={e=>setInstitution(e.target.value)} placeholder="Universitas / Institusi" required/></label><label>Bidang keahlian<input placeholder="Ekonomi Makro, AI, Pendidikan..."/></label><label className="full">Biografi singkat<textarea rows={5} placeholder="Tuliskan profil profesional singkat"/></label><button className="btn btn-primary">Simpan profil</button></form></div></div>}
