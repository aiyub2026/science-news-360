import Link from "next/link"; import {getStorySummary,getStoryTitle,Locale,stories} from "@/lib/content";
export function StoryCard({story,locale,index=0}:{story:typeof stories[number],locale:Locale,index?:number}){return <article className="card"><div className="card-visual" style={index>2?{filter:`hue-rotate(${index*35}deg)`}:undefined}></div><div className="card-body"><span className="eyebrow">{story.category}</span><h3 className="serif"><Link href={`/${locale}/article/${story.slug}`}>{getStoryTitle(story,locale)}</Link></h3><p className="summary">{getStorySummary(story,locale)}</p><div className="meta"><span>Science News 360</span><span>6 min</span></div></div></article>}
