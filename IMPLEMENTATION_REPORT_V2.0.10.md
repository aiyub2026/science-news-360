# Implementation Report V2.0.10

The In-Article Related Content Engine is implemented for dynamically published CMS articles. The public renderer splits the saved HTML body into three stable segments and inserts related cards after approximately 45% and 84% of top-level content blocks. Editors may use automatic ranking or choose each related article manually. Only published records in the same locale are eligible.
