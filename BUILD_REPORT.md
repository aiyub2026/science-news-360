# BUILD REPORT

Container build could not run because the internal npm registry returned HTTP 404 for official packages. Package versions are pinned for reproducible installation on the Mac validation environment. Final LOCK requires successful `npm install`, `npm run build`, and `npm run dev` on Mac.
